program latihan;
uses crt;

var
    nama : integer;
    keaktifan : real;
    tugas : char;
    ujian : string;

begin
   clrscr;
    {input nama}
    write ('masukan nama :');
    readln (nama);

    write('masukan nilai keaktifan :');
    readln(keaktifan);
    
    write('masukan nilai tugas ');
    readln(tugas);

    write('masukan nilai ujian :');

end.
        